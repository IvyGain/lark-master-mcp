{
  "name": "@ivygain/lark-master-brain",
  "version": "0.1.0",
  "description": "Claude Agent SDK brain for Lark Master — receives prompts from the Worker and drives lark-cli.",
  "private": true,
  "type": "module",
  "main": "./dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/server.js",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.10",
    "@larksuiteoapi/lark-mcp": "^0.4.0",
    "@modelcontextprotocol/sdk": "^1.0.4",
    "execa": "^9.5.1",
    "hono": "^4.6.14",
    "@hono/node-server": "^1.13.7",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=22"
  }
}
